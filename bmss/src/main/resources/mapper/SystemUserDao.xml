<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qsz.bmss.dao.SystemUserDao">
    <select id="selectSystemMenuByUserId" resultType="com.qsz.bmss.domain.SystemMenu" parameterType="long">
        SELECT DISTINCT sm.* FROM sys_user_role sur
            LEFT JOIN  sys_role_menu srm ON sur.role_id = srm.role_id
            LEFT JOIN sys_menu sm ON srm.menu_id = sm.menu_id
            WHERE sur.user_id = #{userId}
    </select>

    <select id="selectSysRoleByUserId" resultType="com.qsz.bmss.domain.SystemRole" parameterType="long">
        SELECT sr.* FROM sys_role sr
		LEFT JOIN sys_user_role se ON se.role_id = sr.role_id
		WHERE se.user_id = #{userId}
    </select>


</mapper>